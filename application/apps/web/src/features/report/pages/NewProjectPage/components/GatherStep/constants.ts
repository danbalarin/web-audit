export const MAX_SPEED = 10; // MB/s
import { GatherStep } from ".";
import { StepView } from "../../types/StepView";

export const GATHER_STEP: StepView = {
	component: GatherStep,
	label: "Gather Data",
};
